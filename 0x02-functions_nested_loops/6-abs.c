#include "main.h"

/**
 * _abs - Computes the absolute value of an integer.
 * @a: integer that will be calculated
 * Return: return to a
 */

int _abs(int a)
{
	if (a < 0)

		(a *= -1);

	return (a);
}
